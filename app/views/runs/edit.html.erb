<% content_for :title, t("runs.edit.title") %>

<h1 class="font-bold text-4xl mbe-4"><%= t("runs.edit.title") %></h1>

<%= render "form", run: @run %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to t("runs.edit.show_this_run"), @run, class: "btn" %>
  <%= link_to t("runs.edit.back_to_runs"), runs_path, class: "btn" %>
</div>
