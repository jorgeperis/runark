<% content_for :title, t("runs.new.title") %>

<h1 class="font-bold text-4xl mbe-4"><%= t("runs.new.title") %></h1>

<%= render "form", run: @run %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to t("runs.new.back_to_runs"), runs_path, class: "btn" %>
</div>
